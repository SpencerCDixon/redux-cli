import React, { Component, PropTypes } from 'react';

const propTypes = {
};

class <%= name %> extends Component {
  render() {
    return (

    )
  }
}

<%= name %>.propTypes = propTypes;
export default <%= name %>;
